root = "../.."
tmp_dir = "registration-service/output"

[build]
cmd = "go build -gcflags \"all=-N -l\" -o ../output/main.exe ."
entrypoint = "registration-service/output/main.exe"
include_dir = ["registration-service/src", "backend-lib/src"]

[log]
time = true

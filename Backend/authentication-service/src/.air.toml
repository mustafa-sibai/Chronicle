root = "../.."
tmp_dir = "authentication-service/output"

[build]
cmd = "go build -gcflags \"all=-N -l\" -o ../output/main.exe ."
entrypoint = "authentication-service/output/main.exe"
include_dir = ["authentication-service/src", "backend-lib/src"]

[log]
time = true
